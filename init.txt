--Primera corrida--
npm install && npm run dev [Install React dependencies]
composer install [Install Laravel dependencies]
crear usuario userCourt p@$$w0rd
crear base de datos courtDB
(Modificar el .env si es necesario)
php artisan migrate:fresh --seed
php artisan storage:link
--Antes de correr la app--
php artisan serve -> Run the app